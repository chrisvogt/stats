{
  "name": "stats",
  "version": "0.7.0",
  "scripts": {
    "postinstall": "bower cache clean && bower install"
  },
  "license": "MIT",
  "dependencies": {
    "bower": "^1.3.2"
  },
  "engines": {
    "node": "0.10.x"
  }
}
